<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--  Revit-like colour palette (adjust if you want another shade)  -->
    <Color x:Key="RevitPrimaryColor">#2E6FA8</Color>
    <!--  main blue  -->
    <Color x:Key="RevitAccentColor">#BFD7EF</Color>
    <!--  pale blue accent  -->
    <Color x:Key="RevitSurface">#FFFFFF</Color>
    <!--  card / surface  -->
    <Color x:Key="RevitBackgroundColor">#F4F6F8</Color>
    <!--  app background  -->
    <Color x:Key="RevitBorder">#E2E6EA</Color>
    <Color x:Key="RevitMutedText">#55606A</Color>

    <SolidColorBrush x:Key="RevitPrimary" Color="{StaticResource RevitPrimaryColor}" />
    <SolidColorBrush x:Key="RevitAccent" Color="{StaticResource RevitAccentColor}" />
    <SolidColorBrush x:Key="RevitSurfaceBrush" Color="{StaticResource RevitSurface}" />
    <SolidColorBrush x:Key="RevitBackground" Color="{StaticResource RevitBackgroundColor}" />
    <SolidColorBrush x:Key="RevitBorderBrush" Color="{StaticResource RevitBorder}" />
    <SolidColorBrush x:Key="RevitMutedTextBrush" Color="{StaticResource RevitMutedText}" />

    <!--  Common metrics  -->
    <Thickness x:Key="ControlPadding">10,6</Thickness>
    <CornerRadius x:Key="ControlCornerRadius">8</CornerRadius>

    <!--  Button style  -->
    <Style x:Key="RevitButton" TargetType="Button">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{StaticResource RevitPrimary}" />
        <Setter Property="Padding" Value="{StaticResource ControlPadding}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        Background="{TemplateBinding Background}"
                        CornerRadius="{StaticResource ControlCornerRadius}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="6,2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.95" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform Y="1" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Opacity" Value="0.9" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{StaticResource RevitBorderBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource RevitMutedTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Margin" Value="4" />
    </Style>

    <!--  ComboBox style  -->
    <Style x:Key="RevitComboBox" TargetType="ComboBox">
        <Setter Property="Padding" Value="{StaticResource ControlPadding}" />
        <Setter Property="BorderBrush" Value="{StaticResource RevitBorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource RevitMutedTextBrush}" />
        <Setter Property="Background" Value="{StaticResource RevitSurfaceBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border
                            x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource ControlCornerRadius}">
                            <DockPanel>
                                <ContentPresenter
                                    x:Name="ContentSite"
                                    Margin="6,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                                <ToggleButton
                                    Width="36"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    DockPanel.Dock="Right"
                                    Focusable="False"
                                    IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Path
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Data="M 0 0 L 4 4 L 8 0 Z"
                                        Fill="{StaticResource RevitMutedTextBrush}" />
                                </ToggleButton>
                            </DockPanel>
                        </Border>
                        <Popup
                            x:Name="PART_Popup"
                            AllowsTransparency="True"
                            Focusable="False"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            Placement="Bottom"
                            PopupAnimation="Fade">
                            <Border
                                Margin="0,6,0,0"
                                Background="{StaticResource RevitSurfaceBrush}"
                                BorderBrush="{StaticResource RevitBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                                <ScrollViewer MaxHeight="240" CanContentScroll="True">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  CheckBox style  -->
    <Style x:Key="RevitCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource RevitMutedTextBrush}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <Border
                            x:Name="Box"
                            Width="20"
                            Height="20"
                            VerticalAlignment="Center"
                            Background="{StaticResource RevitSurfaceBrush}"
                            BorderBrush="{StaticResource RevitBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4">
                            <Path
                                x:Name="CheckMark"
                                Data="M2,9 L7,14 L18,3"
                                Stroke="{StaticResource RevitPrimary}"
                                StrokeEndLineCap="Round"
                                StrokeStartLineCap="Round"
                                StrokeThickness="2"
                                Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" RecognizesAccessKey="True" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource RevitAccent}" />
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  ListView / ListBox shared item style  -->
    <Style x:Key="RevitListView" TargetType="ListView">
        <!--  Основные параметры  -->
        <Setter Property="Background" Value="#FFFFFF" />
        <Setter Property="BorderBrush" Value="#E2E6EA" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="#55606A" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Margin" Value="6" />

        <!--  Контейнер элементов  -->
        <!--<Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Margin" Value="2,1" />
                    <Setter Property="Padding" Value="8,4" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border
                                    x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    SnapsToDevicePixels="True">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
        -->
        <!--  Hover  -->
        <!--
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#F0F5FA" />
                                    </Trigger>
        -->
        <!--  Selected  -->
        <!--
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#2E6FA8" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
        -->
        <!--  Disabled  -->
        <!--
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>

        -->
        <!--  GridViewColumnHeader оформление  -->
        <!--
        <Setter Property="GridView.ColumnHeaderContainerStyle">
            <Setter.Value>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Background" Value="#F4F6F8" />
                    <Setter Property="Foreground" Value="#55606A" />
                    <Setter Property="BorderBrush" Value="#E2E6EA" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="Padding" Value="6,4" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                </Style>
            </Setter.Value>
        </Setter>-->
    </Style>
    <!--  ListBoxItems use same visual  -->
    <!--<Style
        x:Key="RevitListBoxItem"
        BasedOn="{StaticResource RevitListItem}"
        TargetType="ListBoxItem" />-->

    <!--  ListView style  -->
    <!--<Style x:Key="RevitListView" TargetType="ListView">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource RevitListItem}" />
    </Style>-->

    <!--  ListBox style  -->
    <!--<Style x:Key="RevitListBox" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource RevitListBoxItem}" />
    </Style>-->

    <!--  Small card for layout  -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource RevitSurfaceBrush}" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Margin" Value="8" />
        <Setter Property="BorderBrush" Value="{StaticResource RevitBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
</ResourceDictionary>